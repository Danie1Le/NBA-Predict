# Core API dependencies (lightweight)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Data processing (essential)
pandas>=1.5.0
numpy>=1.21.0

# Traditional ML (fast startup models)
scikit-learn>=1.1.0
xgboost>=1.7.0

# Deep learning frameworks (for advanced models) - KEPT FOR RESUME VALUE
torch>=1.12.0
tensorflow>=2.10.0
